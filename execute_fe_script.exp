#!/usr/bin/expect -f

set timeout 600
set password "qz%:Ls3+6LauyU"

spawn ssh -o HostKeyAlgorithms=+ssh-rsa -o PubkeyAcceptedAlgorithms=+ssh-rsa -o StrictHostKeyChecking=no root@66.116.224.31 "chmod +x /root/deploy_frontend.sh && /root/deploy_frontend.sh"

expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
