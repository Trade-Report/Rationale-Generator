#!/usr/bin/expect -f

set timeout 300
set password "qz%:Ls3+6LauyU"

spawn scp -o HostKeyAlgorithms=+ssh-rsa -o PubkeyAcceptedAlgorithms=+ssh-rsa -o StrictHostKeyChecking=no admin_web/frontend_deploy.zip root@66.116.224.31:/root/frontend_deploy.zip

expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
